# CLAUDE.md

## Bash
- Do NOT prefix Bash commands with `cd` into the project root. The Bash tool's working directory already persists and starts in this project directory (`/Users/tompitts/Library/Mobile Documents/com~apple~CloudDocs/dphil icloud/CanopyAI`). Use relative or absolute paths directly. If a command needs a different subdirectory, use a subshell: `(cd subdir && cmd)`.

- Think for as long as you need, but be succinct when answering. Use your words precisely and economically. 
